# 王者荣耀苹果微信省标监控

```bash
python app.py
```

打开 `http://127.0.0.1:8765`。服务监听在 `0.0.0.0:8765`，局域网设备可以通过本机 IP 访问。

页面会展示国服王者荣耀全英雄在苹果微信区的省标最低分，也就是省榜第 100 名分数。数据来自公开第三方接口 `sapi.run`，每 10 分钟自动刷新一次，也可以手动刷新。

Windows 登录后自动启动：

```bat
install_startup.bat
```

取消自启动：

```bat
uninstall_startup.bat
```
